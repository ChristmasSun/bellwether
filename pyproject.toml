[project]
name = "bellwether"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "beautifulsoup4",
    "sqlalchemy[asyncio]",
    "aiosqlite",
    "apscheduler",
    "pydantic",
]
